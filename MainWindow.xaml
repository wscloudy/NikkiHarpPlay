<Window x:Class="MidiKeyPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MIDI 转模拟按键" Height="760" Width="1100"
        MinHeight="680" MinWidth="980"
        Icon="Assets/app.ico">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,8,8,0"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,2,8,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,2,8,2"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <Button Content="选择 MIDI 文件" Command="{Binding SelectMidiCommand}" DockPanel.Dock="Left"/>
            <TextBox Text="{Binding FilePath}" IsReadOnly="True"/>
        </DockPanel>

        <TextBlock Grid.Row="1" Text="{Binding FileInfo}" Margin="0,8,0,0" FontWeight="SemiBold"/>

        <Grid Grid.Row="2" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.05*"/>
                <ColumnDefinition Width="1.25*"/>
                <ColumnDefinition Width="1.35*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="轨道列表" Grid.Column="0">
                <DataGrid ItemsSource="{Binding TrackInfos}" AutoGenerateColumns="False" CanUserAddRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                        <DataGridTextColumn Header="轨道" Binding="{Binding TrackIndex}" IsReadOnly="True" Width="80"/>
                        <DataGridTextColumn Header="音符数量" Binding="{Binding NoteCount}" IsReadOnly="True" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Header="音符到按键映射" Grid.Column="1">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="新增" Command="{Binding AddMappingCommand}"/>
                        <Button Content="删除选中" Command="{Binding RemoveMappingCommand}"/>
                        <Button Content="保存 JSON" Command="{Binding SaveMappingsCommand}"/>
                        <Button Content="加载 JSON" Command="{Binding LoadMappingsCommand}"/>
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding Mappings}" SelectedItem="{Binding SelectedMapping, Mode=TwoWay}"
                              AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="音符" Binding="{Binding NoteName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                            <DataGridTextColumn Header="MIDI 编号" Binding="{Binding NoteNumber}" IsReadOnly="True" Width="90"/>
                            <DataGridTextColumn Header="按键" Binding="{Binding KeyName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </GroupBox>

            <GroupBox Header="播放选项" Grid.Column="2">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="速度倍率" Grid.Row="0" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding SpeedOptions}" SelectedItem="{Binding SpeedMultiplier}"/>

                        <TextBlock Text="移调半音 (-12~12)" Grid.Row="1" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TransposeSemitones, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="八度位移 (-4~4)" Grid.Row="2" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding OctaveShift, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="开始倒计时 (秒)" Grid.Row="3" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CountdownSeconds, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Tap 按下时长 (ms)" Grid.Row="4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TapDurationMs, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="和弦分解间隔 (ms)" Grid.Row="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding ArpeggioIntervalMs, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="过滤短音符 (ms)" Grid.Row="6" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding MinNoteDurationMs, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="最小按键间隔 (ms)" Grid.Row="7" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding MinKeyIntervalMs, UpdateSourceTrigger=PropertyChanged}"/>

                        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Margin="0,8,0,0">
                            <CheckBox Content="只取主旋律（同一时间点保留最高音）" IsChecked="{Binding MelodyOnly}"/>
                            <CheckBox Content="和弦分解模式" IsChecked="{Binding ArpeggioMode}"/>
                            <CheckBox Content="和弦从高到低分解" IsChecked="{Binding ArpeggioHighToLow}"/>
                            <CheckBox Content="超出映射音域自动折叠" IsChecked="{Binding FoldOutOfRangeNotes}"/>
                            <CheckBox Content="自动最佳移调" IsChecked="{Binding AutoTuneOnOpen}"/>
                            <CheckBox Content="无限暖暖" IsChecked="{Binding UseGameWindowMessageMode}"/>
                        </StackPanel>

                        <TextBlock Grid.Row="9" Grid.ColumnSpan="2" Margin="0,12,0,0" TextWrapping="Wrap"
                                   Foreground="#666"
                                   Text="F8 为全局停止热键。若目标游戏不接受普通 SendInput，工具只会记录提示，不做更底层输入规避。"/>
                    </Grid>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="3" Margin="0,12,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="160"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="开始演奏" Command="{Binding StartCommand}"/>
                <Button Content="{Binding PauseResumeText}" Command="{Binding PauseResumeCommand}"/>
                <Button Content="停止" Command="{Binding StopCommand}"/>
            </StackPanel>
            <ProgressBar Grid.Column="1" Height="22" Minimum="0" Maximum="{Binding ProgressMaximum}" Value="{Binding ProgressValue}" Margin="8,0"/>
            <TextBlock Grid.Column="2" Text="{Binding TimeText}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        </Grid>

        <GroupBox Grid.Row="4" Header="日志">
            <TextBox Text="{Binding LogText}" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap" FontFamily="Consolas"/>
        </GroupBox>
    </Grid>
</Window>
